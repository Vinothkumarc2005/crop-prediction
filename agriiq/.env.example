# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_DB=agriiq
POSTGRES_USER=agriiq
POSTGRES_PASSWORD=agriiq_secret

# ── FastAPI backend ───────────────────────────────────────────────────────────
# psycopg v3 connection string (NOT JDBC format)
DATABASE_URL=postgresql://agriiq:agriiq_secret@localhost:5432/agriiq

# JWT — change this in production to a 256-bit random string
JWT_SECRET=change_me_in_production_must_be_at_least_256_bits_long_abcdefghijklmnop
JWT_EXPIRATION_SECONDS=86400

# ML model directory
MODEL_DIR=ml_models

# ── Provider switches ─────────────────────────────────────────────────────────
# mock = built-in synthetic data (default)
# live = real external API (requires API keys below)
NDVI_PROVIDER=mock
SOIL_PROVIDER=mock

# ── Live API keys (only used when provider=live) ──────────────────────────────
# Sentinel Hub (NDVI from Sentinel-2)
# Register at: https://www.sentinel-hub.com/
SENTINEL_HUB_CLIENT_ID=
SENTINEL_HUB_CLIENT_SECRET=

# Agmarknet (Mandi prices)
# Apply at: https://agmarknet.gov.in/
AGMARKNET_API_KEY=

# ── Frontend ──────────────────────────────────────────────────────────────────
VITE_API_BASE_URL=http://localhost:8000/api
